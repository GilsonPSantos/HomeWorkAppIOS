import UIKit

extension Home {
    struct ViewModel {
        let title: String
        let percentageCompletedValue: CGFloat
        let percentagelabel: String
        let percentageColor: UIColor
    }
}
